{
  "name": "daysite",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Astro template that renders a Day project website: localized landing page, screenshot gallery, downloads, and the hosted web app, from the repo's own store/ metadata.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "test": "node --test 'scripts/*.test.mjs'",
    "sample": "node scripts/generate-appindex.mjs samples samples --storefront samples/day-storefront.json && node scripts/assemble-gallery.mjs samples/screenshots samples"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "@tailwindcss/vite": "^4.1.17",
    "astro": "^5.16.4",
    "qrcode-generator": "^2.0.4",
    "smol-toml": "^1.4.2",
    "tailwindcss": "^4.1.17"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "pagefind": "^1.4.0",
    "typescript": "^5.9.3"
  }
}
